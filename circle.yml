machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - sudo apt-get install python-dev
    - sudo pip install awsebcli 

deployment:
  development:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy --profile default
