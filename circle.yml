machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - sudo yum install aws-cli

deployment:
  development:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy --profile default
