machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - sudo pip install 'awsebcli==3.7.4' --force-reinstall

deployment:
  development:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy --profile default
